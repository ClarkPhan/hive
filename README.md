# Setup
##### `node: v14.15.4`
##### `npm: v6.14.10`

## Step 1
### `npm install`
Run in root directory to install required npm packages.

## Step 2
### `npm start`
Runs the app in the development mode.
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.